// change the name from config-example.ts to config.ts once you make changes!
// (I wish there would be a simple and gentle way but there isnt for gitignore(??))

export const config = {
    dns_server_domain: ".your.domain.com", //needs to start with dot!
    password: "super-long-and-secure-password!!"
}